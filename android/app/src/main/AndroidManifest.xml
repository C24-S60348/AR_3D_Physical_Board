<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools">

    <!-- Camera permission required for AR -->
    <uses-permission android:name="android.permission.CAMERA"/>
    <!-- Internet for loading remote GLB models -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- i-GB requires ARCore. Scanning the printed cards is the point of the
         app, so Google Play should say "Your device isn't compatible with this
         version" on the listing rather than let someone install it and find the
         scanner dead. That is why an Oppo A3 Pro and a Realme tablet cannot
         install: ARCore does not support them.

         Play rejects a bundle whose com.google.ar.core meta-data disagrees with
         this flag, so the two below must stay in step. checkArSupport() still
         guards the scanner at runtime as a safety net. -->
    <uses-feature android:name="android.hardware.camera.ar" android:required="true"/>
    <uses-feature android:name="android.hardware.camera" android:required="true"/>

    <application
        android:label="i.-GB"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">

        <!-- "required" matches the uses-feature above, so Play's device filter
             and the ARCore installability check agree. Play then also installs
             Google Play Services for AR alongside the app. -->
        <meta-data
            android:name="com.google.ar.core"
            android:value="required"
            tools:replace="android:value"/>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:screenOrientation="sensorPortrait"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"/>
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2"/>
    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>

</manifest>

<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools">

    <!-- Camera permission required for AR -->
    <uses-permission android:name="android.permission.CAMERA"/>
    <!-- Internet for loading remote GLB models -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- i-GB requires ARCore. Google Play then hides the app from any device
         that does not declare the android.hardware.camera.ar system feature —
         including the "Your device isn't compatible with this version" notice
         shown on the store listing itself, before the user ever taps Install.
         That upfront signal is preferred over letting an unsupported phone
         install the app and find out at runtime. -->
    <uses-feature android:name="android.hardware.camera.ar" android:required="true"/>
    <uses-feature android:name="android.hardware.camera" android:required="true"/>

    <application
        android:label="i.-GB"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">

        <!-- "required" matches the uses-feature above so Play Store's device
             filter and the ARCore installability check agree. -->
        <meta-data
            android:name="com.google.ar.core"
            android:value="required"
            tools:replace="android:value"/>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:screenOrientation="sensorPortrait"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"/>
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2"/>
    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>

</manifest>

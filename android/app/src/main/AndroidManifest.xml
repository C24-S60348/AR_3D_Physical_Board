<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools">

    <!-- Camera permission required for AR -->
    <uses-permission android:name="android.permission.CAMERA"/>
    <!-- Internet for loading remote GLB models -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- ARCore requires camera hardware.

         Deliberately NOT declaring android.hardware.camera.ar. That is a system
         feature flag the manufacturer bakes into the device image, and plenty of
         ARCore-certified phones never declare it, so requiring it hides the app
         from devices ARCore genuinely supports (the Oppo A3 Pro among them).
         Play still restricts the app to certified devices through the
         com.google.ar.core "required" meta-data below, which filters against
         Google's own ARCore device list. -->
    <uses-feature android:name="android.hardware.camera" android:required="true"/>

    <application
        android:label="i.-GB"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">

        <!-- Override plugin's "optional" to "required" — ARCore must be present -->
        <meta-data
            android:name="com.google.ar.core"
            android:value="required"
            tools:replace="android:value"/>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:screenOrientation="sensorPortrait"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"/>
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2"/>
    </application>

    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>

</manifest>
